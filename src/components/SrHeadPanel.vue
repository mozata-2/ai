<template>
  <section class="sr-head">
    <h1 class="page-title">索拉 视频生成</h1>
    <p class="page-desc">索拉 · 文本生成视频 · 高品质视频创作工具</p>
  </section>
</template>

<script setup>
</script>

<style scoped>
.sr-head {
  width: 100%;
  box-sizing: border-box;
  padding: 2px 6px 8px;
  background: transparent;
}
.page-title {
  margin: 0;
  font-size: 20px;
  line-height: 1.15;
  font-weight: 700;
  letter-spacing: 0.2px;
  color: var(--text-primary, #FFFFFF);
  transition: color var(--theme-dur) var(--theme-ease);
}
.page-desc {
  margin: 9px 0 0;
  font-size: 14px;
  line-height: 1.85;
  color: var(--text-faint, #98A2B3);
  transition: color var(--theme-dur) var(--theme-ease);
}
</style>
